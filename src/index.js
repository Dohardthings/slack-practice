'use strict';

import MessageHistoryView from 'message-history-view';

// export default function () {

  // function showHistory() {
  //   const messageHistory = new MessageHistoryView(this.info.messages);
  //   const element = document.querySelector(`.history`);
  //   element.appendChild(messageHistory);
  // }
